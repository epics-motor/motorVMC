# Use the following lines if motorVMC was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorVMC was built outside motor
#!MOTOR_VMC=
#!-include $(MOTOR_VMC)/configure/RELEASE.local
